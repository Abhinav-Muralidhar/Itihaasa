<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_parchment" />

    <!-- Pre-Android 12 fallback: show the logo immediately (Compose intro animates after first draw). -->
    <item
        android:width="168dp"
        android:height="168dp"
        android:gravity="center"
        android:drawable="@drawable/splash_logo_fg" />
</layer-list>

